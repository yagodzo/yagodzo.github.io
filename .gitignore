# Служебные файлы Obsidian, которые не нужны в репозитории сайта.
# Папка Excalidraw/ намеренно НЕ игнорируется: там лежат исходники
# схем (.excalidraw.md). В постах используются только экспортированные
# PNG, но без исходников схему потом не отредактировать.
.obsidian/workspace.json

# Результат сборки Jekyll
_site/
.jekyll-cache/
.jekyll-metadata
